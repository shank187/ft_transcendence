FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Expose Vite's default port
EXPOSE 5173

# Run the dev server and expose it externally to the host machine
CMD ["npm", "run", "dev", "--", "--host"]